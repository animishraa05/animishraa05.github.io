okay so html does the main content of website  meaning it mainly defines the structure of website ,
waht and how the will look like and it does them by the tags and attributes



##browsers

waht are browsers and why are they used 
so browsers are like tools to properly render the content of server where the client has hit meaning rendering the content of wesbsite on which
client has sent packets, meaning any one can make browsers if they can write the code for rendering the website content.

so basically html gives us some primitives to create websites like this meaning not style it but create it.
some of the popular tags are 
<head>
<body>
<title>
<div>
<span>
<p>
<a>
input
<button>
<center>
 

 <html>

 ok so this tag is used for any html file , isnt really important as even if i write the website without it the browser itself will inject it 
also the head tag and the body tag 
 <body>
 -- the whole content of your website is generally inside this body tag 
 <head>
 we can put some metadata info inside the head tag (waht is metadata , the title tag could be one , data about the data is metadata _)


