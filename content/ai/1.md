# AI

artificial intelligence can be referred as an algorithm derived by its constraints onto the help of representative models that can think, reason, and analyse. 
or we can just say artificial intelligence is training the models to do something better than what we are doing, 

* its like constraints expose representation, they are models targeted at thinking, perception and reasoning, and they are 
nothing without representation .

like let us imagine there is a farmer, goose and grain who are willing to go by a bridge or sum and they gotta take one by one, so we can certainly say that constraints expose representation.

as we are programmers so everything is about programs only in the end
so *AI* is algorithm enabled by constraints exposed by representation that support model targeted at thinking, perception and action.
once more :-

algorithm enabled by constraints which is exposed by representation that supports models targeted at thinking, percpetion and action.

## history

in 1950 alan turing wrote the Turing test. 
Paper written by Marvin Minsky (1960)

so it was just a question about integration calculus where they used safe transformation and heuristic transformation a method that often work isnt guarenteed to work, when the answer is like not the algorithm but the procedure or an attempt towards something useful, like  
